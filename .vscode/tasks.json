{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar y ejecutar en terminal externa",
            "type": "shell",
            "command": "cmake --build . --target run_external",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}